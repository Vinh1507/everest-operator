---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: opsrequests.everest.percona.com
spec:
  group: everest.percona.com
  names:
    kind: OpsRequest
    listKind: OpsRequestList
    plural: opsrequests
    shortNames:
    - ops
    singular: opsrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .spec.engineRef.name
      name: Engine
      type: string
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.progress
      name: Progress
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpsRequest is the Schema for the opsrequests API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpsRequestSpec defines the desired operation
            properties:
              clusters:
                description: |-
                  clusters is a list of database clusters that belong to this group.
                  When any cluster in the group reconciles, all other clusters will be triggered to reconcile as well.
                items:
                  description: ClusterReference defines a reference to a database
                    cluster
                  properties:
                    name:
                      description: name is the name of the database cluster
                      minLength: 1
                      type: string
                    namespace:
                      description: Namespace of the database cluster (defaults to
                        OpsRequest namespace)
                      type: string
                    type:
                      description: type is the type of database cluster (e.g., "pxc",
                        "psmdb", "postgresql", "starrocks")
                      enum:
                      - pxc
                      - psmdb
                      - postgresql
                      - clickhouse
                      - starrocks
                      - cassandra
                      type: string
                  required:
                  - name
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              force:
                description: Force indicates whether to force the operation even if
                  it might cause downtime
                type: boolean
              groupRef:
                description: GroupRef references the target group
                properties:
                  name:
                    description: Name of the group
                    type: string
                  namespace:
                    description: Namespace of the group (defaults to OpsRequest namespace)
                    type: string
                required:
                - name
                type: object
              params:
                additionalProperties:
                  type: string
                description: Params contains operation-specific parameters
                type: object
              timeout:
                default: 30m
                description: |-
                  Timeout specifies the maximum time to wait for the operation to complete
                  Format: 1h, 30m, 10s
                type: string
              type:
                description: Type specifies the type of operation
                enum:
                - Start
                - Stop
                - Restart
                - Switchover
                - VerticalScaling
                - HorizontalScaling
                - VolumeExpansion
                - Reconfiguring
                - Upgrade
                - Backup
                - Restore
                - Expose
                - Custom
                type: string
            required:
            - groupRef
            - type
            type: object
          status:
            description: OpsRequestStatus defines the observed state
            properties:
              clusterStatus:
                description: ClusterStatus contains status for each cluster
                items:
                  description: OpsRequestClusterStatus defines the status of an operation
                    on a specific cluster
                  properties:
                    clusterName:
                      description: ClusterName is the name of the cluster
                      type: string
                    clusterNamespace:
                      description: ClusterNamespace is the namespace of the cluster
                      type: string
                    message:
                      description: Message provides human-readable information
                      type: string
                    output:
                      additionalProperties:
                        type: string
                      description: Output contains operation-specific output data
                      type: object
                    phase:
                      description: Phase represents the current phase of the operation
                        on this cluster
                      type: string
                    progress:
                      description: Progress indicates the operation progress (0-100)
                      format: int32
                      type: integer
                    reason:
                      description: Reason provides a machine-readable explanation
                      type: string
                  required:
                  - clusterName
                  - clusterNamespace
                  type: object
                type: array
              completionTime:
                description: CompletionTime is when the operation completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message provides human-readable information
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation observed by
                  the controller
                format: int64
                type: integer
              output:
                additionalProperties:
                  type: string
                description: Output contains operation-specific output data
                type: object
              phase:
                description: Phase represents the current phase of the operation
                enum:
                - Pending
                - Processing
                - Succeeded
                - Failed
                - Canceled
                type: string
              progress:
                description: Progress indicates the operation progress (0-100)
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              reason:
                description: Reason provides a machine-readable explanation
                type: string
              startTime:
                description: StartTime is when the operation started
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: clustergroups.everest.percona.com
spec:
  group: everest.percona.com
  names:
    kind: ClusterGroup
    listKind: ClusterGroupList
    plural: clustergroups
    shortNames:
    - cg
    singular: clustergroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.totalClusters
      name: Clusters
      type: integer
    - jsonPath: .status.readyClusters
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterGroup is the Schema for the clustergroups API.
          It represents a group of database clusters that should be reconciled together.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterGroup
            properties:
              clusters:
                description: |-
                  clusters is a list of database clusters that belong to this group.
                  When any cluster in the group reconciles, all other clusters will be triggered to reconcile as well.
                items:
                  description: ClusterReference defines a reference to a database
                    cluster
                  properties:
                    name:
                      description: name is the name of the database cluster
                      minLength: 1
                      type: string
                    type:
                      description: type is the type of database cluster (e.g., "pxc",
                        "psmdb", "postgresql", "starrocks")
                      enum:
                      - pxc
                      - psmdb
                      - postgresql
                      - clickhouse
                      - starrocks
                      - cassandra
                      type: string
                  required:
                  - name
                  - type
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              paused:
                default: false
                description: |-
                  paused indicates whether the cluster group reconciliation is paused.
                  When true, the controller will not reconcile this ClusterGroup or its member clusters.
                type: boolean
            required:
            - clusters
            type: object
          status:
            description: status defines the observed state of ClusterGroup
            properties:
              clusterGenerations:
                additionalProperties:
                  format: int64
                  type: integer
                description: |-
                  clusterGenerations tracks the generation of each cluster
                  Key is cluster name, value is the generation
                type: object
              clusters:
                description: clusters contains the status of each cluster in the group
                items:
                  description: ClusterStatus represents the status of a single cluster
                    in the group
                  properties:
                    generation:
                      description: generation is the current generation of the cluster
                      format: int64
                      type: integer
                    lastReconcileTime:
                      description: lastReconcileTime is the last time the cluster
                        was reconciled
                      format: date-time
                      type: string
                    message:
                      description: message provides additional information about the
                        cluster status
                      type: string
                    name:
                      description: name is the name of the cluster
                      type: string
                    observedGeneration:
                      description: observedGeneration is the generation observed by
                        the cluster controller
                      format: int64
                      type: integer
                    phase:
                      description: phase represents the current phase of the cluster
                      type: string
                    ready:
                      description: ready indicates if the cluster is ready
                      type: boolean
                    type:
                      description: type is the type of the cluster
                      type: string
                  required:
                  - name
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              conditions:
                description: conditions represent the current state of the ClusterGroup
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastReconcileTime:
                description: lastReconcileTime is the last time the group was reconciled
                format: date-time
                type: string
              observedGeneration:
                description: |-
                  observedGeneration is the generation observed by the controller.
                  It is used to determine if the spec has changed and reconciliation is needed.
                format: int64
                type: integer
              phase:
                description: |-
                  phase represents the current phase of the ClusterGroup.
                  Possible values: "Pending", "Reconciling", "Ready", "PartiallyReady", "Failed"
                enum:
                - Pending
                - Reconciling
                - Ready
                - PartiallyReady
                - Failed
                type: string
              readyClusters:
                description: readyClusters is the number of clusters that are ready
                type: integer
              totalClusters:
                description: totalClusters is the total number of clusters in the
                  group
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
